import React from 'react'
import spinner from '../assets/spin.gif'
const Spinner = () => {
    return (
        <div>
            <img src={spinner} width='700' alt='spinner' />
        </div>
    )
}

export default Spinner
